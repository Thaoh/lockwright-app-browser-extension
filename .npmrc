# Auth/registry / npm-client settings only (pnpm v11). Other settings live in
# pnpm-workspace.yaml. engine-strict makes npm honor package.json#engines /
# #devEngines before install proceeds.
engine-strict=true
